```json
{
  "name": "capacitor-app",
  "version": "1.0.0",
  "description": "A basic Capacitor project with GitHub Actions build",
  "main": "index.js",
  "scripts": {
    "start": "npx @capacitor/cli serve",
    "build": "mkdir -p www/src && cp index.html www/index.html && cp src/index.js www/src/index.js",
    "capacitor:add:android": "npx @capacitor/cli add android",
    "capacitor:add:ios": "npx @capacitor/cli add ios",
    "capacitor:sync": "npx @capacitor/cli sync",
    "capacitor:open:android": "npx @capacitor/cli open android",
    "capacitor:open:ios": "npx @capacitor/cli open ios"
  },
  "dependencies": {
    "@capacitor/core": "latest",
    "@capacitor/cli": "latest",
    "@capacitor/android": "latest",
    "@capacitor/ios": "latest"
  },
  "devDependencies": {}
}
```